# DESIGN.md — rocky-todo

> 이 문서는 rocky-todo 웹 UI(`src/ui/`)의 **시각 계약**이다. 에이전트가 UI 를 만들거나
> 고칠 때 여기를 먼저 읽는다. `AGENTS.md`(에이전트용 레포 규칙) · `docs/rocky-todo.md`(사람용
> 설치·운영 문서)와 나란한 세 번째 단일 출처다.
>
> 기준은 **main 디자인**이다(사용자 결정 2026-07-28). 팔레트는
> `src/ui/styles.test.ts` 의 SNAPSHOT 이 값 자체를 고정한다 — 색을 바꾸려면 사용자
> 승인과 함께 그 표를 갱신해야 하고, 이 문서의 색표도 따라간다. 개선은 이 기준 위에서
> 조금씩, 사용자와 실물을 보며 진행한다(§8).

## Overview

rocky-todo 는 마케팅 페이지가 아니라 **한 사람이 하루 종일 띄워두는 도구**다. 로컬
데몬이 들고 있는 보드를 사람과 여러 AI 에이전트가 **동시에** 쓴다. 화면의 단 하나의
임무는 이것이다 — *지금 무엇이, 누구 손에서 움직이고 있는가.*

기질은 **은은한 저대비 콘솔**이다. 웜 니어블랙 캔버스 위에 요소들이 낮은 대비로
가라앉아 있고, 지금 중요한 것(제목, 처리중 뱃지)만 앞으로 나온다. 행 높이 32px 의
조밀한 밀도, 장식 없음, 얇은 윤곽선 알약(칩·뱃지)이 이 UI 의 손맛이다. 구분선은
배경과 거의 구별되지 않는 수준(≈1.4:1)으로 깔린다 — 구조는 선이 아니라 **여백과
흐림의 위계**가 만든다.

색의 핵심은 **행위자 온도**다. 앰버(`--warm`)는 에이전트가 한 일, 아이스 블루(`--cool`)는
사람이 한 일. 두 색은 장식이 아니라 **의미**이고, 이 제품이 다른 todo 앱과 갈리는
지점이다. 이름은 프로젝트 헤일메리의 에리디언 로키에서 왔다 — 뜨거운 암모니아 대기와
지구의 대기.

**Key Characteristics**

- **저대비 위계** — 물러날 것은 `--faint`/`--muted` 로 가라앉히고, 중요한 것만
  `--text`/`--warm` 으로 세운다. 흐림이 이 UI 의 주된 위계 장치다
- **채도 있는 액센트는 정확히 둘**(`--warm` / `--cool`)이고 둘 다 행위자 의미 전용.
  `--p1`~`--p3` · `--ok` · `--handoff` 는 상태 신호이지 브랜드색이 아니다
- **웜 캔버스** — 다크(`#16110c`)·라이트(`#faf6f0`) 모두. `#000000` 도 `#ffffff` 도
  캔버스로 쓰지 않는다 (라이트의 `--surface` `#ffffff` 은 캔버스가 아니라 한 단계 올린 면)
- **칩·뱃지는 얇은 윤곽선 알약** — 1px `--line` 테두리 + mono 10px. 채운 면이 아니다
- **구조는 mono, 사람 글은 sans** — 워드마크·번호·칩·뱃지·타임스탬프는 전부 mono.
  사용자가 쓴 제목·본문만 sans
- **오프라인 전용** — 외부 리소스(CDN 폰트·아이콘·이미지) 금지. 데몬이 전부 로컬 서빙
- 다크가 기본, **라이트 모드는 그 톤의 대응**이다. 새 시각 결정은 두 테마 값을 함께 정한다

## 1. 구현 — Tailwind 유틸 + 토큰

스타일링은 Tailwind v4 유틸리티로 컴포넌트에 붙는다. 값은 전부 CSS 커스텀
프로퍼티(토큰)에서 온다 — 유틸은 `text-(--muted)` 처럼 토큰을 참조하고, 색값을
유틸에 직접 쓰지 않는다.

`src/ui/styles.css` 에 남는 것은 **토큰 2블록, base 레이어 리셋, 유틸로 표현 못 하는
소수**(체크박스 ::after, md-line 후손 셀렉터, tone-* 동적 마커, keyframes, dvh 블록)뿐이다.

### 실전 규칙 (마이그레이션에서 실측으로 배운 것)

- **같은 프로퍼티의 유틸을 한 요소에 두 번 주지 않는다** — 나열 순서가 아니라 CSS 생성
  순서로 결정돼 비결정적이다. 공통 문자열(`CHIP` 등)에는 색을 두지 말고 변형마다 명시한다
- **동적 클래스 조합 금지** — Tailwind 는 정적 스캔이라 변수 보간 클래스는 생성되지
  않는다. 리터럴 매핑(`PRIO_CHIP`)을 쓴다. 주석에 유틸 예시를 쓸 때도 스캐너가 후보로
  집을 수 있으니 표기를 조심한다
- **preflight 금지** — `@import "tailwindcss"` 전체가 아니라 theme/utilities 레이어만.
  수제 리셋은 `@layer base` 안에 있어야 유틸이 이긴다
- 시맨틱 마커 클래스(`todo-row`, `is-done`, `chip` …)는 유틸과 병기해 남긴다

## 2. Colors

원본은 `src/ui/styles.css` 의 두 토큰 블록이고, `src/ui/styles.test.ts` 의 SNAPSHOT 이
동일 값을 고정한다. 셋(코드·테스트·이 표)이 어긋나면 테스트가 먼저 운다.

### 가드 방식 — 스냅샷(현상 고정)

WCAG 대비 기준을 계산해 잠그는 방식을 검토했다 폐기했다(2026-07-28) — 기준을
만족시키려 바닥을 일괄로 올리자 저대비 위계가 무너져 "정신없는" 화면이 됐다. 지금은
**값 자체를 고정**한다: 우연한 드리프트는 막고, 의도적 변경은 사용자 승인 + SNAPSHOT
갱신으로 한다.

알려진 트레이드오프 — `--faint`(≈2.6:1)·`--line`(≈1.4:1) 등은 WCAG AA 에 못 미친다.
이것은 선택된 디자인이다: 물러남이 이 UI 의 위계 장치이고, 그 값이 읽혀야 하는 자리에는
애초에 쓰지 않는다.

### 표면

| 토큰 | DARK | LIGHT |
| --- | --- | --- |
| `--bg` | `#16110c` | `#faf6f0` |
| `--surface` | `#1f1811` | `#ffffff` |
| `--surface-2` | `#282017` | `#f2ebe1` |

### 텍스트

| 토큰 | DARK | LIGHT | 용도 |
| --- | --- | --- | --- |
| `--text` | `#e9dfd2` | `#241c14` | 제목, 본문 |
| `--muted` | `#9c8d7c` | `#6b5c4a` | 부수 텍스트, 칩, 체크박스 테두리 |
| `--faint` | `#6b5f51` | `#756753` | 마이크로라벨 — 가장 물러난 층 |

### 행위자 온도 — 이 제품의 정체성

| 토큰 | DARK | LIGHT | 의미 |
| --- | --- | --- | --- |
| `--warm` | `#e8a33d` | `#6b4600` | **에이전트가 한 일** |
| `--warm-dim` | `#8a6526` | `#8b6019` | 같은 계열의 물러난 단계 (뱃지 테두리, 섹션 라벨) |
| `--cool` | `#7ec8d8` | `#195564` | **사람이 한 일** |
| `--cool-dim` | `#47707a` | `#52828d` | 같은 계열의 물러난 단계 |

### 상태 신호

| 토큰 | DARK | LIGHT | 용도 |
| --- | --- | --- | --- |
| `--p1` | `#e2634f` | `#b3311c` | 우선순위 1, 에러, 마감 초과 |
| `--p2` | `#e8a33d` | `#6b4600` | 우선순위 2 (앰버 = `--warm` 과 같은 값) |
| `--p3` | `#c8b45a` | `#6f6215` | 우선순위 3 |
| `--ok` | `#86b06c` | `#3f6b26` | 체크된 체크박스 (배경) |
| `--handoff` | `#93c5fd` | `#1f5aa8` | 핸드오프 "보냈다" — doing(앰버)과 다른 상태라 파랑 |
| `--handoff-dim` | `#1e3a5f` | `#d3e4fb` | 핸드오프 칩 배경 |

### 경계선·기타

| 토큰 | DARK | LIGHT | 용도 |
| --- | --- | --- | --- |
| `--line` | `#382c1f` | `#d6cab9` | 모든 구분선·칩 테두리 — 배경에 가라앉는 은은한 선 |

흐림·스크림은 토큰이 아니라 규칙에 값이 박혀 있다 — 테마 변형이 없어서다.

| 자리 | 규칙 | 값 |
| --- | --- | --- |
| 드로어 뒤 배경 | `.drawer-backdrop` | `rgba(10, 7, 4, 0.55)` |
| 보관 항목 흐림 | `.is-archived` | `opacity: 0.45` |
| 갱신 끊긴 doing 뱃지 | `.doing-badge.is-stale` | `opacity: 0.55` |

## 3. Typography

**mono 가 기계, sans 가 사람.** 이 UI 에 두 종류의 행위자가 있다는 사실을 타이포가
그대로 말한다.

| 역할 | 스택 | 쓰는 곳 |
| --- | --- | --- |
| `--mono` | `ui-monospace, "SF Mono", SFMono-Regular, Menlo, monospace` | 워드마크, `#N`, 칩, 뱃지, 타임스탬프, 섹션 라벨, 인라인 코드 |
| `--sans` | `-apple-system, …, "Pretendard", sans-serif` | todo 제목, 노트 본문, 댓글 — **사용자가 쓴 글** |

**웹폰트를 함부로 들이지 않는다.** 오프라인 제약 + 한글 콘텐츠 때문이다.

### 크기

| px | 쓰는 곳 |
| --- | --- |
| 18 | 드로어 제목 |
| 14 | 본문 기본 |
| 13 | 노트/댓글 본문, 드로어 설명, 워드마크·테마 토글 |
| 12 | 부수 텍스트, 히스토리 행, actor 칩 |
| **10** | 마이크로라벨 (mono + 넓은 자간) — 링크 상태, 섹션 라벨, 칩, 뱃지, 타임스탬프 |
| 16 | 모바일 입력 필드 전용 (iOS 자동 확대 방지) |

### 자간

mono 마이크로라벨은 자간을 넓힌다(`0.14em`~`0.22em`) — 대문자 mono 는 자간이 넓을수록
글자 구분이 쉽다. 10px 콘솔 라벨의 가독성은 자간이 지탱한다.

## 4. Layout

**빽빽한 콘솔.** 여백으로 숨 쉬는 문서가 아니다.

- **행 높이 32px**, 행 내부 gap 8px, 칩 간 6px, 패널 안쪽 10~16px, 섹션 간 26px
- 3분할 그리드 `200px / 1fr / 300px` (보드 · todo · 노트)
- 좁은 화면(≤900px)에서 세로 스택 — 사이드바는 가로 스크롤 보드 칩 행

## 5. Components

### todo 행 (`.todo-row`)

`[체크박스] [#N] [제목] [칩…] [댓글 뱃지] [핸드오프 칩] [doing 뱃지]`

- 클릭 영역: 체크박스(토글) · `#N`(ref 복사) · **제목**(상세 열기) · 링크 칩 · 댓글 뱃지
- hover 에 `--surface` 면. 완료는 취소선 + `--faint`, 보관은 `.is-archived` 흐림
- 좁은 화면에서 칩이 다음 줄로 흐른다 (`flex-wrap`)

### 칩 (`.chip`)

윤곽선 알약 — `1px solid --line` + mono 10px + `--muted`. 색 변형은 테두리·글자색을
함께 덮는다(우선순위 = `--pN`, 링크 = `--cool` + hover 시 테두리도 `--cool`, 핸드오프 =
`--handoff-dim` 배경). 채운 면 칩은 이 디자인의 어법이 아니다.

### doing 뱃지 (`.doing-badge`)

처리 중인 행위자와 경과 시간 — **행위자 온도가 가장 직접 드러나는 자리.**
행위자 dim 색 테두리(`.tone-warm` → `--warm-dim`, `.tone-cool` → `--cool-dim`) +
currentColor 8% 틴트 + 펄스 점. 갱신이 끊기면 `.is-stale` 로 흐려지고 펄스가 멈춘다. `prefers-reduced-motion` 에서 펄스 정지(`motion-reduce`).

### 드로어

Radix Dialog 셸(포커스 트랩·aria·스크롤 락·Escape) + 기존 시각. 우측 440px(모바일
전폭), `.drawer-backdrop` 오버레이. 입력 중 Escape 는 드로어를 닫지 않는다.

### 상단 바

워드마크 · SSE 링크 상태(`LINK ♪`) · 테마 토글(`◐●○`) · 보관됨 토글 · actor 칩.
`♪` 는 **바꾸지 않는다** — 로키는 음악으로 말한다.

## 6. 반응형

- 분기 하나: `@media (max-width: 900px)` (유틸은 `max-[900px]:`)
- 터치 타깃 44×44 — 조작 컨트롤에 적용, 히트 영역은 맞닿되 겹치지 않게
- 입력 필드는 모바일에서 16px (iOS 자동 확대 방지)

## 7. Do's and Don'ts

### Do

- 새 색이 필요하면 먼저 기존 토큰으로 되는지 본다. 만들면 **두 테마 값 + SNAPSHOT** 을
  함께 갱신한다
- 행위자 구분에는 `actorTone()` — 색을 직접 고르지 않는다
- 물러나야 할 것은 `--faint`/`--muted`/흐림으로 — 그게 이 디자인의 위계다
- 시각을 바꾸는 작업은 데모 데몬을 띄워 **실물로** 확인받는다
  (`ROCKY_TODO_PORT=8993 ROCKY_TODO_DIR=/tmp/rt-demo ROCKY_TODO_EXPOSE=lan bun run src/daemon.ts`)

### Don't

- **세 번째 채도색을 들이지 않는다** — warm/cool 둘로 부족하면 형태·위치로 푼다
- **`#000000`/`#ffffff` 를 캔버스로 쓰지 않는다**
- 칩·뱃지를 채운 면으로 바꾸지 않는다 — 윤곽선 알약이 이 UI 의 어법이다
- 이모지를 상태 표시에 쓰지 않는다 (`♪`·🪨·💬 등 기존 것은 정체성/관례로 유지)
- 같은 프로퍼티 유틸을 한 요소에 겹치지 않는다 (§1 실전 규칙)
- SNAPSHOT 을 사용자 승인 없이 고치지 않는다

## 8. 개선 후보 (조금씩, 실물 확인과 함께)

main 톤을 지키면서 갈 수 있는 방향들. 착수 전 사용자와 실물 기준으로 논의한다.

1. **굵기 스케일** — 지금 400/600/700 이 산발적. 계단을 정리하면 흐림 위계를 보완한다
2. **반경 수렴** — 3·4·6·7·8·10px 혼재 → 소수 단계로
3. **번들 가변 mono** — 구조 계층의 서체 (조건: 가변 woff2, 오픈 라이선스, tabular)
4. **온도 띠 등 시그니처 탐색** — 보드 `rocky-todo#23` 참고
5. 접근성 개선이 필요해지면 — 일괄 상향이 아니라 **자리별로**, 위계를 지키며
   (지난 시도의 교훈: 바닥을 일괄로 올리면 위계가 무너진다. 탐색 이력은 git 의
   `docs/superpowers/specs/2026-07-27-ui-contrast-light-mode-design.md` 히스토리에 있다)
