# DESIGN.md — rocky-todo

> 이 문서는 rocky-todo 웹 UI(`src/ui/`)의 **시각 계약**이다. 에이전트가 UI 를 만들거나
> 고칠 때 여기를 먼저 읽는다. `AGENTS.md`(에이전트용 레포 규칙) · `FEATURES.md`(사람용
> 기능 문서)와 나란한 세 번째 단일 출처다.
>
> 값은 전부 실측·검증된 것이다. 팔레트는 `src/ui/styles.test.ts` 가 CSS 를 파싱해
> 잠근다 — 이 문서와 코드가 어긋나면 테스트가 먼저 운다.

## Overview

rocky-todo 는 마케팅 페이지가 아니라 **한 사람이 하루 종일 띄워두는 도구**다. 로컬
데몬이 들고 있는 보드를 사람과 여러 AI 에이전트가 **동시에** 쓴다. 화면의 단 하나의
임무는 이것이다 — *지금 무엇이, 누구 손에서 움직이고 있는가.*

기질은 **빽빽하고 절제된 콘솔**이다. 행 높이 32px, 조밀한 간격, 장식 없음. 색 온도는
**따뜻하다** — 캔버스가 중성 검정도 순백도 아니라 웜 니어블랙(`--bg` #16110c)과 웜
아이보리(#faf6f0)다. 차가운 정밀함과 따뜻한 표면을 같이 쓰는 자리이고, 이 조합이 이
UI 의 인상을 결정한다.

색의 핵심은 **행위자 온도**다. 앰버(`--warm`)는 에이전트가 한 일, 아이스 블루(`--cool`)는
사람이 한 일. 두 색은 장식이 아니라 **의미**이고, 이 제품이 다른 todo 앱과 갈리는
지점이다. 이름은 프로젝트 헤일메리의 에리디언 로키에서 왔다 — 뜨거운 암모니아 대기와
지구의 대기.

**Key Characteristics**

- **위계는 면·모서리·여백·굵기가 만든다.** 선과 흐림은 위계 장치가 **아니다** — 이 문서의
  제1원칙이며, 아래 모든 규칙이 여기서 파생된다
- **채도 있는 액센트는 정확히 둘**(`--warm` / `--cool`)이고 둘 다 행위자 의미 전용. 세 번째
  채도색은 없다 (`--p1`~`--p3`, `--ok`, `--handoff` 는 상태 신호이지 브랜드색이 아니다)
- **웜 캔버스** — 다크·라이트 양쪽 다. `#000000` 도 `#ffffff` 도 캔버스로 쓰지 않는다
  (`--surface` 의 라이트 값 `#ffffff` 은 캔버스가 아니라 한 단계 올린 면이다)
- **표면 사다리는 의도적으로 미묘하다** — 단계당 1.07~1.18. 면은 *구역을 나누되 소리치지
  않는다*
- **경계선은 헤어라인** — 캔버스 대비 1.4~1.5. 구조를 지는 장치가 아니라 면 위의 속삭임
- **구조는 mono, 사람 글은 sans** — 워드마크·번호·칩·뱃지·타임스탬프는 전부 mono. 사용자가
  쓴 제목·본문만 sans
- **오프라인 전용** — 외부 리소스(CDN 폰트·아이콘·이미지) 금지. 데몬이 전부 로컬에서 서빙한다
- 다크·라이트 **양 테마 필수**. 어느 한쪽만 성립하는 결정은 하지 않는다

---

## 1. 위계 (Elevation & Hierarchy)

**이 절이 이 문서의 중심이다.**

위계를 만드는 것은 순서대로 — **면(표면 단계) → 모서리 → 여백 → 굵기**.

위계를 만들지 **않는** 것 — **선**과 **흐림(opacity·저대비)**.

### 왜 선과 흐림이 아닌가

- **흐림**: 접근성과 정면충돌한다. 흐릿함으로 위계를 만들면 그 흐림이 곧 대비 미달이 된다.
  실제로 이 UI 가 그 함정에 빠진 적이 있다 — `--faint` 2.58, `--line` 1.18 로 "안 읽힌다"는
  지적을 받았고, 고치려고 일괄로 올렸더니 이번엔 위계가 평평해져 "정신 없다"가 됐다
- **선**: 개수만큼 시끄러워진다. 이 UI 엔 경계선이 17곳 있다. 선 하나를 세게 만들면 17곳이
  동시에 세진다

### 표면 사다리

세 단계뿐이고 단계 차이는 작다. 이건 **의도**다 — 참고로 Linear 의 4단계 사다리도 단계당
1.01~1.10, 전체 폭 1.20 으로 우리(1.17)와 사실상 같다. 면은 눈에 띄게 다른 게 아니라
*그 자리에 있음을 알 정도만* 다르다.

| 단계 | 토큰 | 용도 |
| --- | --- | --- |
| 0 (캔버스) | `--bg` | 페이지 배경, 입력창 안쪽 |
| 1 | `--surface` | 상단 바, 노트 카드, 드로어, 행 hover |
| 2 | `--surface-2` | 활성 보드 항목, 인라인 코드, 드로어 버튼 |

**면을 건너뛰지 않는다.** 캔버스 위에 곧바로 2단계를 올리지 않는다.

---

## 2. Colors

값은 `src/ui/styles.css` 의 두 토큰 블록이 원본이다. 아래는 그 사본이며 어긋나면 CSS 가 맞다.

### 대비 기준 — 쓰임이 기준을 정한다

토큰마다 하한이 다르다. **어떤 CSS 속성에 쓰이느냐**로 정해진다.

| 부류 | 하한 | 근거 |
| --- | --- | --- |
| `color:` 로 쓰이는 토큰 | **4.5** | WCAG 2.1 본문 대비 |
| `border:`/`background:` 전용이면서 **사용자가 조작하는 컨트롤**의 경계 | **3.0** | WCAG 1.4.11 비텍스트 대비 |
| 장식적 구분선 | **하한 없음** | 1.4.11 적용 대상이 아니다. 값은 아래 "헤어라인" 규칙이 정한다 |

측정은 각 테마의 세 배경(`--bg`/`--surface`/`--surface-2`) 중 **최악값** 기준이다.
`styles.test.ts` 가 이 표를 강제한다.

> **주의** — 예전에 "장식적 구분선 ≥ 2.2" 라는 하한을 둔 적이 있다. WCAG 근거가 없는
> 임의값이었고, 그 규칙 때문에 `--line` 이 2.7 까지 올라가 화면이 시끄러워졌다. 폐기했다.
> **지표를 규칙으로 굳히기 전에 디자인 입장을 먼저 정한다.**

### 표면

| 토큰 | DARK | LIGHT |
| --- | --- | --- |
| `--bg` | `#16110c` | `#faf6f0` |
| `--surface` | `#1f1811` | `#ffffff` |
| `--surface-2` | `#282017` | `#f2ebe1` |

### 텍스트

| 토큰 | DARK | LIGHT | 용도 |
| --- | --- | --- | --- |
| `--text` | `#e9dfd2` | `#241c14` | 제목, 본문 |
| `--muted` | `#9c8d7c` | `#6b5c4a` | 부수 텍스트, 체크박스 테두리 |
| `--faint` | `#978775` | `#756753` | 마이크로라벨(mono 11px) |

### 행위자 온도 — 이 제품의 정체성

| 토큰 | DARK | LIGHT | 의미 |
| --- | --- | --- | --- |
| `--warm` | `#e8a33d` | `#6b4600` | **에이전트가 한 일** |
| `--warm-dim` | `#b08130` | `#8b6019` | 같은 계열의 물러난 단계 |
| `--cool` | `#7ec8d8` | `#195564` | **사람이 한 일** |
| `--cool-dim` | `#4b7680` | `#52828d` | 같은 계열의 물러난 단계 |

라이트에서 `--warm`/`--cool` 이 한 단계 더 깊은 이유 — 본색이 얕으면 `-dim` 이 설 자리가
없어 둘이 같은 색으로 읽힌다. 다크에서 `--warm`↔`--warm-dim` 은 1.62, 라이트는 1.51 로
비슷하게 벌려 뒀다.

### 상태 신호

| 토큰 | DARK | LIGHT | 용도 |
| --- | --- | --- | --- |
| `--p1` | `#e2634f` | `#b3311c` | 우선순위 1, 에러, 마감 초과 |
| `--p2` | `#e8a33d` | `#6b4600` | 우선순위 2 (앰버 = `--warm` 과 같은 값) |
| `--p3` | `#c8b45a` | `#6f6215` | 우선순위 3 |
| `--ok` | `#86b06c` | `#3f6b26` | 체크된 체크박스 (배경) |
| `--handoff` | `#93c5fd` | `#1f5aa8` | 핸드오프 "보냈다" |
| `--handoff-dim` | `#1e3a5f` | `#d3e4fb` | 핸드오프 칩 배경 |

`--handoff` 가 파랑인 이유 — doing(앰버)과 **다른 상태**임을 색으로 갈라야 한다.

### 경계선

| 토큰 | DARK | LIGHT | 용도 |
| --- | --- | --- | --- |
| `--line` | `#3f3123` (1.50) | `#d6cab9` (1.50) | 패널·행 구분선 — 헤어라인 |
| `--line-strong` | `#876a4b` | `#9a7e59` | 입력·버튼·select 등 **조작 가능한** 컨트롤 경계 |

**헤어라인 목표: 캔버스 대비 1.4~1.5.** 참고로 Linear 헤어라인은 1.36 이다.
`--line-strong` 만 3.0 하한을 지킨다 — 컨트롤 경계는 WCAG 1.4.11 대상이라 실제 근거가 있다.

### 그 외

| 토큰 | DARK | LIGHT | 용도 |
| --- | --- | --- | --- |
| `--scrim` | `rgba(10, 7, 4, 0.55)` | `rgba(36, 28, 20, 0.35)` | 드로어 뒤 배경 |
| `--dim-archived` | `0.45` | `0.55` | 보관 항목 흐림 |
| `--dim-stale` | `0.55` | `0.65` | 30분 이상 갱신 없는 doing 뱃지 |

라이트에서 흐림이 더 센 이유 — 같은 0.45 면 밝은 배경 위에서 보관 행이 다크(3.72) 대비
2.79 로 떨어진다. **흐림은 위계 장치가 아니라 "지금 안 중요함" 신호다.** 새로 쓰지 말 것.

---

## 3. Typography

**mono 가 기계, sans 가 사람.** 이 UI 에 두 종류의 행위자가 있다는 사실을 타이포가 그대로
말한다.

| 역할 | 스택 | 쓰는 곳 |
| --- | --- | --- |
| `--mono` | `ui-monospace, "SF Mono", SFMono-Regular, Menlo, monospace` | 워드마크, `#N`, 칩, 뱃지, 타임스탬프, 섹션 라벨, 인라인 코드 |
| `--sans` | `-apple-system, BlinkMacSystemFont, "Segoe UI", "Apple SD Gothic Neo", "Pretendard", sans-serif` | todo 제목, 노트 본문, 댓글 — **사용자가 쓴 글** |

**웹폰트를 함부로 들이지 않는다.** 오프라인 제약(번들 필수) + 한글 콘텐츠 때문이다. 개성
있는 라틴 디스플레이 서체는 대부분 한글이 없어서 제목에 쓰면 한영 혼용 줄이 무너진다.
투자할 여지가 있는 곳은 **mono 하나**다 — 구조를 전부 지고 있어 그것만 바꿔도 인상이 바뀐다.

### 크기

| px | 쓰는 곳 |
| --- | --- |
| 18 | 드로어 제목 |
| 14 | 본문 기본 (`body`) |
| 13 | 노트/댓글 본문, 드로어 설명 |
| 12 | 부수 텍스트, 히스토리 행 |
| **11** | 마이크로라벨 (mono) — **하한이다. 그 아래로 내리지 않는다** |
| 16 | 모바일 입력 필드 전용 (iOS 자동 확대 방지) |

### 굵기

굵기는 **위계 장치다.** 지금은 600/700 만 산발적으로 쓰여 스케일이 없다 — 채워야 할 갭이다
(§9).

| 굵기 | 쓰는 곳 |
| --- | --- |
| 400 | 본문 기본 |
| 600 | 활성 보드, 읽지 않은 배지, 댓글 작성자 |
| 700 | 워드마크, 드로어 제목 |

### 자간

mono 마이크로라벨은 자간을 넓힌다(`0.14em`~`0.22em`). 대문자 mono 는 자간이 넓을수록
글자 구분이 쉬워 **11px 에서 가독성에 기여한다.** 크기를 줄이는 대신 자간을 좁히지 않는다.

---

## 4. Layout

**빽빽한 콘솔.** 여백으로 숨 쉬는 문서가 아니다.

| 값 | 쓰는 곳 |
| --- | --- |
| 2px | 사이드바 항목 간격 |
| 6px | 행 내부 요소 간격(칩 등) |
| 8px | 행 요소 기본 간격 |
| 10~14px | 패널 안쪽 여백 |
| 16~26px | 패널 바깥 여백 |
| 26px | 섹션(todo 그룹) 간격 |

- **행 높이 32px** (`.todo-row min-height`) — 이 밀도가 기준이다
- 3분할 그리드 `200px / 1fr / 300px` (보드 · todo · 노트)
- 좁은 화면에서 세로 스택으로 접힌다 (§8)

---

## 5. Shapes

**모서리는 위계 장치다** — 면과 함께 "이건 하나의 덩어리"를 만든다. 그런데 지금 8종
(3·4·6·7·8·10px·50%·999px)이 제각각 쓰여 규율이 없다. 아래로 수렴시킨다.

| 토큰 | 값 | 쓰는 곳 |
| --- | --- | --- |
| `sm` | 4px | 체크박스, 인라인 코드 |
| `md` | 6px | 버튼, 입력, 행 hover, select |
| `lg` | 10px | 카드(노트 카드 등) |
| `pill` | 999px | 칩, 뱃지, actor 칩 |
| `dot` | 50% | 펄스 점 |

3px · 7px · 8px 는 위 다섯으로 흡수한다. **새 반경을 만들지 않는다.**

---

## 6. Components

### todo 행 (`.todo-row`)

`[체크박스] [#N] [제목] [메타 칩…] [댓글] [핸드오프] [doing 뱃지]`

- 좌측 클러스터(체크박스+번호)는 **붙어 있어야 한다** — 둘 사이가 벌어지면 제목이 밀려
  목록 전체가 헐거워진다. 번호 박스는 `min-width: 2.2em`(`#999` 까지), 오른쪽 정렬
- hover 에 `--surface` 면을 깐다 (선이 아니라 면)
- 완료된 행은 제목에 취소선 + `--faint`

### 칩 (`.chip`)

우선순위·라벨·마감·링크. 한 행에 **5개까지** 붙는다.

**칩은 면으로 만든다 — 윤곽선을 두르지 않는다.** 작은 크기에서 윤곽선은 채움보다 훨씬
시끄럽고, 한 행에 5개면 선 5개가 된다.

### doing 뱃지 (`.doing-badge`)

처리 중인 행위자와 경과 시간. **행위자 온도가 가장 직접적으로 드러나는 자리다.**

- `tone-warm`(에이전트) / `tone-cool`(사람) — 배경은 `color-mix(in srgb, var(--warm) 8%, transparent)`
- 펄스 점이 살아 있음을 표시. `prefers-reduced-motion` 에서 정지
- 30분 이상 갱신 없으면 `--dim-stale` 로 흐려지고 펄스가 멈춘다

### 드로어 (`.drawer`)

우측에서 나오는 상세. `--scrim` 이 뒤를 덮고 `body.is-drawer-open` 이 배경 스크롤을 잠근다.

### 상단 바 (`.topbar`)

워드마크 · SSE 링크 상태(`LINK ♪`) · 테마 토글 · 보관됨 토글 · actor 칩.

`♪` 는 **바꾸지 않는다.** 로키는 음악으로 말한다 — 이 UI 에서 캐릭터를 가장 많이 지고
있는 기호다.

---

## 7. Do's and Don'ts

### Do

- 위계가 필요하면 **면·모서리·여백·굵기** 중에서 고른다
- 경계선은 **헤어라인**(캔버스 대비 1.4~1.5)으로 둔다. 보이되 읽히지 않는 수준
- 조작 가능한 컨트롤의 경계에만 `--line-strong`(≥3.0)을 쓴다
- 새 색이 필요하면 **먼저 기존 토큰으로 되는지** 본다. 만들면 `styles.test.ts` 의 분류
  상수에 등록해야 한다
- 행위자 구분이 필요한 자리엔 `actorTone()` 을 쓴다 — 색을 직접 고르지 않는다
- 다크·라이트 **두 값을 같이** 정한다. 한쪽만 정의하면 대칭성 테스트가 실패한다
- 아이콘은 문자 글리프 또는 번들 SVG. **외부 CDN 금지**

### Don't

- **흐림(opacity·저대비)으로 위계를 만들지 않는다.** 흐림은 "지금 안 중요함" 신호 전용
  (`--dim-archived` / `--dim-stale`)
- **선을 세게 만들어 구조를 세우지 않는다.** 선이 안 보여서 문제라면 면·여백을 먼저 의심한다
- **세 번째 채도색을 들이지 않는다.** warm/cool 둘로 부족하면 형태나 위치로 푼다
- **`#000000`/`#ffffff` 를 캔버스로 쓰지 않는다.** 웜 캔버스가 이 UI 의 온도다
- **작은 요소에 윤곽선을 두르지 않는다** — 칩·뱃지는 면으로
- **11px 아래로 내려가지 않는다**
- 새 `border-radius` 값을 만들지 않는다 (§5 의 다섯 중에서 고른다)
- 이모지를 상태 표시에 쓰지 않는다 — 색이 고정돼 톤을 못 받고 OS 마다 모양이 다르다
  (`♪` 와 🪨 파비콘은 예외 — 정체성이다)
- WCAG 근거 없는 숫자를 하한 규칙으로 굳히지 않는다

---

## 8. Responsive

- **분기 하나: `@media (max-width: 900px)`**
- 3분할이 세로 스택으로 접힌다. 사이드바는 가로 스크롤 보드 칩 행이 된다
- **터치 타깃 44×44** — 조작 컨트롤에 적용. 예외는 목록의 `#N` 버튼(26×44)으로, 44px 가로
  폭이 오른쪽 정렬된 번호를 밀어 체크박스와 51px 벌어지기 때문이다. WCAG 2.5.8(24×24)은
  통과한다
- 히트 영역은 **맞닿되 겹치지 않는다** — 겹치면 이웃 컨트롤의 탭을 가로챈다
- 입력 필드는 좁은 화면에서 `16px` (iOS 자동 확대 방지)
- `prefers-reduced-motion: reduce` 에서 모든 펄스 정지

---

## 9. Known Gaps

지금 코드가 이 문서를 아직 못 따라가는 곳. 고칠 때 여기를 지운다.

1. **굵기 스케일이 없다** — 600/700 만 산발적으로 쓰인다. 400/500/600/700 계단을 세워야
   흐림에 기대지 않고 위계가 선다
2. **반경이 8종이다** — §5 의 다섯으로 수렴시켜야 한다
3. **mono 가 무개성이다** — `ui-monospace` 는 OS 기본값이다. 번들 가변 mono 하나가
   구조 전체의 인상을 바꾼다 (조건: 가변 woff2, 오픈 라이선스, tabular 숫자)
4. **컴포넌트 스타일링이 손으로 쓴 CSS 1100줄이다** — Tailwind + Radix 도입 검토는
   `docs/superpowers/specs/2026-07-28-tailwind-shadcn-design.md` 참고. 도입하더라도 이
   문서의 규칙이 상위다

---

## 10. Iteration Guide

1. **한 번에 컴포넌트 하나**를 고친다. §6 의 이름으로 부른다
2. 무언가 안 보인다는 지적을 받으면 **먼저 위계 계층(§1)을 의심**한다. 대비를 올리는 것은
   마지막 수단이다 — 대비를 올리면 그 요소가 물러날 자리를 잃는다
3. 색을 바꿨으면 `bun test src/ui/styles.test.ts` 로 두 테마를 함께 확인한다
4. 새 토큰은 **두 테마 모두** 정의하고 분류 상수에 등록한다
5. 라이트에서만, 또는 다크에서만 성립하는 결정을 하지 않는다
6. 실물을 본다. 이 UI 의 문제는 대부분 정적 분석으로 안 잡힌다 —
   `ROCKY_TODO_PORT=8993 ROCKY_TODO_DIR=/tmp/rt-demo ROCKY_TODO_EXPOSE=lan bun run src/daemon.ts`
7. 게이트: `bun run check` · `bun run typecheck` · `bun test`
