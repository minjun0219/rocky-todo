#!/usr/bin/env bun
/**
 * rocky-todo CLI entry — 파싱과 커맨드 라우팅은 전부 src/cli.ts 에 있다.
 */

import { runCli } from '../src/cli';

runCli().catch((error: unknown) => {
  console.error(error instanceof Error ? error.message : String(error));
  process.exit(1);
});
