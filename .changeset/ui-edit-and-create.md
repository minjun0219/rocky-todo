---
"@minjun0219/rocky-todo": minor
---

웹 UI 에서 제목을 고치고 보드·섹션을 만들 수 있게 했다.

- 상세 드로어의 제목을 클릭하면 입력창으로 바뀐다 (Enter 저장 · Esc 취소). 그동안 설명은
  고칠 수 있는데 제목만 못 고쳤다.
- 사이드바에 `+ 새 보드`, 보드 뷰에 `+ 섹션` 진입점을 추가했다. 전에는 CLI 로만 만들 수
  있어서 브라우저만으로는 새 프로젝트를 시작할 수 없었다.
- 빈 섹션을 만드는 `POST /api/sections` 를 추가했다 — 기존에는 todo 를 만들 때 이름으로
  upsert 되는 길뿐이라 항목 없이 섹션만 둘 수 없었다.
- 보드 key 규칙(공백·`#` 불가) 위반은 서버 메시지를 그대로 입력창 아래 보여준다. 조용히
  실패하면 왜 안 만들어지는지 알 수 없다.
