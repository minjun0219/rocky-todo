---
"@minjun0219/rocky-todo": minor
---

todo/note 에 보드별 번호(#12)를 추가했다. `rocky#12` / `#12` / id / id 접두사를 어디서나
같은 자리에 쓸 수 있고(CLI, MCP `id` 인자, 웹 UI), 웹 UI 에서 번호를 클릭하면 `rocky#12` 가
클립보드에 복사되어 세션에 붙여넣기 좋다. 기존 랜덤 id 는 그대로 기본 키로 남는다.
